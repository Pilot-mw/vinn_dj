{
  "build": {
    "builder": "NIXPACKS.Builder.Python"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "python manage.py migrate --noinput && gunicorn djvin_site.wsgi --log-file -"
  }
}
